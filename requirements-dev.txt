# Test only. Kept out of requirements.txt so the deployed image does not
# carry a test runner and an HTTP client it never uses.
-r requirements.txt

pytest
# Starlette's TestClient prefers httpx2 and falls back to httpx.
httpx2
